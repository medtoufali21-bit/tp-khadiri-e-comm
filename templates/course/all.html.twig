{% extends 'layout.html.twig' %}

{% block title %}Mediatheque{% endblock %}

{% block content %}
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>Mediatheque</h1>
        <a href="{{ path('app_course') }}" class="btn btn-primary">Ajouter un support</a>
    </div>

    <div class="row row-cols-1 row-cols-md-3 g-4">
        {% for course in list %}
            <div class="col">
                <div class="card h-100">
                    <div class="card-body">
                        <h5>{{ course.title }}</h5>
                        <p>{{ course.summary ?: 'Aucun resume.' }}</p>
                    </div>
                    <div class="card-footer d-flex justify-content-between">
                        <a href="{{ path('app_course_show', {identifier: course.id}) }}" class="btn btn-outline-primary btn-sm">Voir</a>
                        {% if course.filePath %}
                            <a href="{{ asset(course.filePath) }}" class="btn btn-outline-secondary btn-sm">Telecharger</a>
                        {% endif %}
                    </div>
                </div>
            </div>
        {% else %}
            <div class="col">
                <div class="alert alert-info">Aucun cours uploade pour le moment.</div>
            </div>
        {% endfor %}
    </div>
{% endblock %}
