{% extends 'layout.html.twig' %}

{% block title %}Panier{% endblock %}

{% block content %}
    <h1>Votre panier</h1>
    {% if cart is empty %}
        <div class="alert alert-info">Votre panier est vide.</div>
        <a href="{{ path('app_catalog_all') }}" class="btn btn-primary">Retour aux formations</a>
    {% else %}
        <div class="table-responsive">
            <table class="table table-striped align-middle">
                <thead>
                    <tr>
                        <th>Cours</th>
                        <th>Format</th>
                        <th>Quantite</th>
                        <th>Prix</th>
                    </tr>
                </thead>
                <tbody>
                    {% for item in cart %}
                        <tr>
                            <td>{{ item.name }}</td>
                            <td>{{ item.format }}</td>
                            <td>{{ item.quantity }}</td>
                            <td>{{ (item.price * item.quantity)|number_format(2, ',', ' ') }} DH</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
        <div class="d-flex justify-content-between align-items-center">
            <h3>Total : {{ total|number_format(2, ',', ' ') }} DH</h3>
            <div>
                <a href="{{ path('app_cart_clear') }}" class="btn btn-outline-secondary">Vider le panier</a>
                <a href="{{ path('app_checkout') }}" class="btn btn-primary">Passer au paiement</a>
            </div>
        </div>
    {% endif %}
{% endblock %}
